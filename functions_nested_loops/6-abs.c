#include "main.h"

/**
 * _abs -   function that computes the absolute value of an integer.
 * @int: absolute value
 * Return: value of an integer.
 */

int _abs(int r)
{
		if (r >= 0 && r <= 0)
		{
			_putchar(r);
			_putchar('\n');
		}
		return (0);
}
